<%= simple_form_for(@guess) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :photo_id %>
    <%= f.input :guess_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
