<% form_tag :action => 'add_comment', :via => :post %>
  <%= text_field 'comment', 'title' %>
  <%= text_field 'comment', 'comment' %>

  <%= submit_tag 'Add Comment' %>

  <%= hidden_field 'commentable', 'commentable', :value => @photo.type %>
  <%= hidden_field 'commentable', 'commentable_id', :value => @photo.id %>